<Window x:Class="SafetensorsViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SafetensorsViewer"
        xmlns:converters="clr-namespace:SafetensorsViewer.Converters"
        xmlns:WpfPlot="http://schemas.scottplot.net/2008/xaml/presentation"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="900"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Closing="Window_Closing"
>
    <Window.Resources>
        <converters:NullToBoolConverter x:Key="NullToBoolConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24*"/>
            <RowDefinition Height="28*"/>
            <RowDefinition Height="352*"/>
            <RowDefinition Height="24*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Menu Height="25" VerticalAlignment="Center" Grid.ColumnSpan="3">
            <MenuItem Header="File">
                <MenuItem Header="Open" Command="{Binding Path=openCMD}"/>
                <MenuItem Header="Save" Command="{Binding Path=saveCMD}"/>
                <MenuItem Header="Save As..." Command="{Binding Path=saveAsCMD}"/>
                <Separator />
                <MenuItem Header="Exit" Click="ExitMenuItem_Click"/>
            </MenuItem>
        </Menu>
        <ToolBar Grid.Row="1" Grid.ColumnSpan="3">
            <ToggleButton Content="Edit Mode" IsChecked="{Binding IsEditModeEnabled}" IsEnabled="{Binding DataMatrix, Converter={StaticResource NullToBoolConverter}}" />
            <TextBlock Text="Brush step:" VerticalAlignment="Center" Margin="8,0,2,0"/>
            <ComboBox Width="80" SelectedItem="{Binding BrushStep}" ItemsSource="{Binding BrushStepOptions}" />
        </ToolBar>
        <StatusBar Height="25" VerticalAlignment="Bottom" Grid.ColumnSpan="3" Grid.RowSpan="2" Grid.Row="2" >
            <StatusBarItem>
                <TextBlock Text="{Binding Status}" />
            </StatusBarItem>
        </StatusBar>
        <WpfPlot:WpfPlot Name="MyPlot" Grid.Column="1"  DataContext="{Binding Path=DataMatrix, RelativeSource={RelativeSource AncestorType={x:Type local:MainWindow}}}" Grid.Row="2" MouseUp="MyPlot_MouseUp" />
        <WpfPlot:WpfPlot Name="MyPlot1" Grid.Column="2"  DataContext="{Binding Path=DataMatrix, RelativeSource={RelativeSource AncestorType={x:Type local:MainWindow}}}" Grid.Row="2"/>
        <TreeView ItemsSource="{Binding Path=TensorKeys}"  Margin="10,10,10,10" Grid.Row="2"  SelectedItemChanged="TreeView_OnSelectedItemChanged" />
    </Grid>
</Window>
