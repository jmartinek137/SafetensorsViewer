<Window x:Class="SafetensorsViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SafetensorsViewer" xmlns:WpfPlot="http://schemas.scottplot.net/2008/xaml/presentation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="900"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Closing="Window_Closing"
>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24*"/>
            <RowDefinition Height="380*"/>
            <RowDefinition Height="24*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Menu Height="25" VerticalAlignment="Center" Grid.ColumnSpan="3">
            <MenuItem Header="File">
                <MenuItem Header="Open" Command="{Binding Path=openCMD}"/>
                <MenuItem Header="Save" Command="{Binding Path=saveCMD}"/>
                <MenuItem Header="Save As..." Command="{Binding Path=saveAsCMD}"/>
                <Separator />
                <MenuItem Header="Exit" Click="ExitMenuItem_Click"/>
            </MenuItem>
        </Menu>
        <StatusBar Height="25" VerticalAlignment="Bottom" Grid.ColumnSpan="3" Grid.RowSpan="2" Grid.Row="1" >
            <StatusBarItem>
                <TextBlock Text="{Binding Status}" />
            </StatusBarItem>
        </StatusBar>
        <WpfPlot:WpfPlot Name="MyPlot" Grid.Column="1"  DataContext="{Binding Path=DataMatrix, RelativeSource={RelativeSource AncestorType={x:Type local:MainWindow}}}" Grid.Row="1" />
        <WpfPlot:WpfPlot Name="MyPlot1" Grid.Column="2"  DataContext="{Binding Path=DataMatrix, RelativeSource={RelativeSource AncestorType={x:Type local:MainWindow}}}" Grid.Row="1"/>
        <TreeView ItemsSource="{Binding Path=TensorKeys}"  Margin="10,10,10,10" Grid.Row="1"  SelectedItemChanged="TreeView_OnSelectedItemChanged" />
    </Grid>
</Window>
